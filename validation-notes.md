# 本机素材库验证记录

- 已在浏览器中将 HTML、Markdown、文本、DOCX 与 PPTX 文件导入当前设备的 IndexedDB 素材库，页面显示本机存储容量与“不同步、不上传”的边界说明。
- HTML 预览在无脚本权限的沙箱 iframe 中渲染；Markdown 已按富文本显示。
- PPTX 已成功提取两张幻灯片文本；复杂版式、动画、图表保留下载原件的回退说明。DOCX 也已在浏览器内渲染正文。
- 多模态端点表单已在 AI 档案页展示图片、语音、音乐和视频独立覆盖项；保存仍沿用基础 AI 档案的模型/API Key 必填校验，防止无有效文本配置时误认为可生成。
- 刷新素材库页面后，先前导入的 HTML、Markdown、DOCX 与 PPTX 均继续列出，验证了当前浏览器 IndexedDB 的本机持久化路径。
- 验收复查发现最初 PPTX 仅提供文字回退，随后接入浏览器 Canvas 渲染；实际运行暴露缺少 Chart.js 的依赖，已补齐。原先手工制作的最小 PPTX 不含演示文稿关系文件，因此已生成标准 Office Open XML 验证稿继续复测。
- 标准 PPTX 验证稿已在 Canvas 中显示实际版式，并成功从第 1 页切换到第 2 页；PDF 验证文件已在浏览器内嵌 PDF 阅读器中显示。
- 在隔离浏览器会话中使用模拟的 OpenAI 兼容文本响应生成 Markdown 后，创作中心自动跳转至素材库，生成文件显示为“AI 文档”分类且已完成 Markdown 预览，验证了生成、归档、分类和预览闭环。
- 使用模拟图片端点返回 PNG 文件后，创作中心将其自动保存为“AI 图片”分类，并在素材库中直接渲染图片预览，验证图片生成的归档与预览闭环。
- 无密钥语音验收中，当前 AI 的独立语音端点、模型 `tts-audit` 与音色 `nova` 已出现在实际 POST 请求体；返回 WAV 文件已归档为“AI 语音”。
- 无密钥音乐验收中，自定义 JSON 模板已将 `{{prompt}}` 与 `{{model}}` 替换为实际内容；裸 Base64 结果按 `audio/mpeg` 归档为 MP3 并分类为“AI 音乐”。
- 无密钥视频验收中，视频端点收到了 multipart 形式的模型与提示词；任务按 `queued → completed` 轮询后下载 MP4 内容，并归档为“AI 视频”。
- 无密钥图片验收中，图片端点收到了专用图片模型、提示词、`1024x1024` 与 `b64_json`；Base64 PNG 结果已识别为图片并归档为“AI 图片”。
- 已在 AI 档案页实际应用“通用 JSON 异步视频”模板：原有视频端点与专用模型保持不变，JSON 请求格式和请求体模板即时写入；保存并刷新后模板标识与取消端点仍保留在当前浏览器。
- 已用隔离浏览器模拟异步视频服务：创建后页面展示 `queued` 状态、百分比进度、任务 ID 与“停止等待”；点击后先向配置的取消端点发出 POST 请求，再停止本机等待。未配置取消端点时仍会仅中止本机轮询，并明确远端任务可能继续。
- 已用隔离浏览器模拟“创建任务 → 完成轮询 → 下载视频”流程；成品自动归档为“AI 视频”。素材详情的生成来源卡展示类型、模型、端点、模板、完整提示词和可展开的实际请求参数，并明确不记录 API Key、只保存在当前浏览器。
- 已依据 GMI Cloud Music 3.0 官方文档添加专属模板：其请求使用 `model` 与嵌套 `payload`，通过 `request_id` 查询状态，并从 `outcome.audio_url` 下载音频。模板可保留用户填写的 HTTPS 代理端点。
- 对 GMI Cloud 官方 console 音乐端点的无凭据 OPTIONS 预检返回 404 且无 CORS 放行头；真实浏览器携带占位 Authorization 的请求也被拦截为 `TypeError: Failed to fetch`。轻聊现在会在模板、创作页和失败提示中明确说明：纯静态页面不能直连，须使用用户自行控制且明确允许 CORS 的 HTTPS 代理，且不建议使用公共 CORS 代理。
- 已在隔离浏览器模拟 GMI Cloud 成功响应的“提交 → request_id 状态查询 → outcome.audio_url 下载”闭环，成品归档为“AI 音乐”；素材来源卡保留模型、模板、端点、歌词和嵌套 payload，不记录 API Key。
