# 只填写允许代理访问的上游 origin（协议 + 域名 + 可选端口），多个用英文逗号分隔。
# 若生成结果放在另一个 CDN 域名，也必须把该 CDN origin 加进来。
QINGLIAO_ALLOWED_UPSTREAM_ORIGINS=https://api.openai.com,https://console.gmicloud.ai

# 只有前端和 API 分开部署时才需要。部署在同一个 Vercel 项目时留空即可。
QINGLIAO_ALLOWED_WEB_ORIGINS=
